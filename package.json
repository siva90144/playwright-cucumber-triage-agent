{
  "name": "pw-cucumber-triage-agent",
  "version": "0.1.0",
  "description": "AI-assisted, rule-based Playwright + Cucumber failure triage CLI with business-friendly reporting.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "bin": {
    "pw-triage": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "prepublishOnly": "npm run typecheck && npm test && npm run build",
    "dev": "tsx src/cli.ts",
    "start": "tsx src/cli.ts"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "stream-json": "^1.9.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}